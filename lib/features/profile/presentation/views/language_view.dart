import 'package:flutter/material.dart';

class LanguageView extends StatelessWidget {
  const LanguageView({super.key});
 static const routName = 'language' ;
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
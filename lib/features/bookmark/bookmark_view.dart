import 'package:flutter/material.dart';

class BookmarkView extends StatelessWidget {
  const BookmarkView({super.key});
 static const routName = 'bookMark';
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}